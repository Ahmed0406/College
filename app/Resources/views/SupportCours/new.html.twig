{% extends 'layout_2.html.twig' %}

{% block content %}
    <h1>Ajouter Cours</h1>

    {{ form_start(form) }}
    <div class="form">
        <ul class="inquiryform">
            <li>
                {{ form_widget(form.nom, {'attr': {
                    'placeholder':'Nom'
                }}) }}
            </li>
                {{ form_widget(form.file.fileFile, {'attr': {

                }}) }}
        </ul>
        {{ form_widget(form) }}
        <button class="btn1 right" type="submit">Ajouter</button>
    </div>
    {{ form_end(form) }}
{% endblock %}
